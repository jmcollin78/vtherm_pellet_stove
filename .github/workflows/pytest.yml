name: Tests

on:
    push:
        branches:
            - main
            - master
    pull_request:

jobs:
    pytest:
        name: Run Pytest
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Checkout vtherm_api
              uses: actions/checkout@v4
              with:
                  repository: KipK/vtherm_api
                  ref: api_algo
                  path: vtherm_api_repo

            - name: Checkout versatile_thermostat
              uses: actions/checkout@v4
              with:
                  repository: jmcollin78/versatile_thermostat
                  ref: v10
                  path: versatile_thermostat_repo

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: "3.13"

            - name: Install dependencies
              run: |
                  python -m pip install --upgrade pip
                  pip install pytest pytest-asyncio pytest-homeassistant-custom-component coverage homeassistant

            - name: Add project dependencies to PYTHONPATH
              run: echo
                  "PYTHONPATH=$PWD:$PWD/vtherm_api_repo/src:$PWD/versatile_thermostat_repo:$PYTHONPATH"
                  >> $GITHUB_ENV

            - name: Run Pytest
              run: pytest tests/

            - name: Upload coverage report
              if: always()
              uses: actions/upload-artifact@v4
              with:
                  name: coverage-report
                  path: .coverage
